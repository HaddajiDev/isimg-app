# Copy to key.properties and fill in. NEVER commit the real file or the .jks —
# both are gitignored. If you lose the keystore you can never publish an update
# to an app already on Play, so keep a backup somewhere safe.
storeFile=upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
